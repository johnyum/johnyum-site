.DS_Store

# Large video masters — never commit (GitHub caps files at 100MB); web uses the HB/faststarted copy
img/Videos/Portfolio_Updated.mp4

# Photo originals — huge, and only the derived webp ship
preso/slides/assets/Luxe/

# Mapbox: only SECRET (sk.) tokens must stay out of the repo. The file below
# holds a PUBLIC (pk.) token, which is designed to ship in client-side source
# and is secured by URL restriction in the Mapbox account, not by secrecy.
# Ignoring it meant it never deployed, so every map on the live site fell back
# to a placeholder. Restrict the token by URL; do not hide it.

# Luxe wall, second set — the 20 source PNGs are 46MB; only the derived
# wall2/*.webp ship
preso/slides/assets/luxe-homes/more/
