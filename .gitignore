.DS_Store

# Large video masters — never commit (GitHub caps files at 100MB); web uses the HB/faststarted copy
img/Videos/Portfolio_Updated.mp4

# Photo originals — huge, and only the derived webp ship
preso/slides/assets/Luxe/

# Mapbox token — never commit; secret (sk.) tokens must not ship client-side
preso/slides/assets/mapbox-config.js
